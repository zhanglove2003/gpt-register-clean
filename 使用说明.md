# 使用说明

本文按一台新机器从零部署的顺序写。所有示例里的密钥、域名、Worker 地址都要替换成你自己的值。

## 1. 需要先准备什么

1. 一个 HeroSMS API Key。
2. 一个已经接入 Cloudflare 的域名，例如 `yourdomain.com`。
3. 一个可用代理，建议本机 HTTP 代理，例如 `127.0.0.1:7897`。没有代理也能配置为空，但 ChatGPT 页面可能打不开。
4. 一台能打开图形浏览器的机器。
5. Node.js 18 或更高版本。
6. Google Chrome 或 Chromium。

在项目目录检查 Node：

```bash
node -v
npm -v
```

如果 `node -v` 小于 18，先升级 Node.js。

## 2. 解压代码并安装依赖

1. 解压代码包。
2. 进入项目目录：

```bash
cd gpt_register
```

3. 安装依赖：

```bash
npm install
```

4. 确认 Chrome 路径。

Linux 常见路径：

```bash
/usr/bin/google-chrome-stable
```

macOS 常见路径：

```bash
/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
```

如果不确定，可以执行：

```bash
which google-chrome-stable
which chromium
```

## 3. 在 HeroSMS 获取配置

注册入口：[https://hero-sms.com/?ref=1292253](https://hero-sms.com/?ref=1292253)

> 促销码 `plucksu`（有效期至 2026-12-31）：注册后使用可获得会员等级 2 级，号码价格降低 15%。

1. 登录 HeroSMS。
2. 找到 API Key。
3. 本项目默认服务代码是 `dr`，对应 OpenAI / ChatGPT。
4. 如果你想固定使用瑞典号码，后面配置里填：

```json
{
  "heroSmsService": "dr",
  "heroSmsCountry": 46,
  "phoneCountryCode": "SE",
  "heroSmsPromptCountrySelection": false
}
```

说明：

- `heroSmsCountry: 46` 是 HeroSMS 的瑞典国家 ID。
- `phoneCountryCode: "SE"` 是网页国家选择用的 ISO 代码。
- `heroSmsPromptCountrySelection: false` 表示启动时不手动选国家。

## 4. 在 Cloudflare 配置临时邮箱

这一步用于接收 OpenAI 邮箱验证码。

### 4.1 域名接入 Cloudflare

1. 登录 Cloudflare。
2. 添加你的域名，例如 `yourdomain.com`。
3. 按 Cloudflare 提示，把域名注册商里的 NS 服务器改成 Cloudflare 给出的 NS。
4. 等待 Cloudflare 显示域名为 Active。

### 4.2 创建 D1 数据库

1. 进入 Cloudflare 后台。
2. 打开 `Workers & Pages`。
3. 打开 `D1 SQL Database`。
4. 点击创建数据库，名字可以填：

```text
mail_db
```

5. 创建完成后，进入这个 D1 数据库。
6. 打开 Console。
7. 把项目里的 `cloudflare-email-worker-schema.sql` 内容粘进去执行。

执行的 SQL 是：

```sql
CREATE TABLE IF NOT EXISTS mailboxes (
  address TEXT PRIMARY KEY,
  created_at INTEGER NOT NULL
);

CREATE TABLE IF NOT EXISTS mails (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  address TEXT NOT NULL,
  sender TEXT,
  subject TEXT,
  raw TEXT NOT NULL,
  received_at INTEGER NOT NULL
);

CREATE INDEX IF NOT EXISTS idx_mails_address_received
  ON mails (address, received_at DESC);
```

### 4.3 创建 Worker

1. 进入 `Workers & Pages`。
2. 点击创建 Worker。
3. Worker 名字随便取，例如：

```text
mail-worker
```

4. 打开 Worker 编辑器。
5. 删除默认代码。
6. 把项目里的 `cloudflare-email-worker.js` 全部粘进去。
7. 保存并部署。

部署后会得到一个地址，例如：

```text
https://mail-worker.your-name.workers.dev
```

后面配置里的 `mailBaseUrl` 就填这个地址。

### 4.4 绑定 D1 和环境变量

进入刚才创建的 Worker，打开 Settings。

1. 找到 Bindings。
2. 添加 D1 database binding。
3. Variable name 必须填：

```text
DB
```

4. D1 database 选择刚才创建的 `mail_db`。
5. 找到 Variables。
6. 添加变量：

```text
MAIL_API_TOKEN=自己设置一个查询密钥
MAIL_DOMAIN=yourdomain.com
```

`MAIL_API_TOKEN` 是脚本访问 Worker 邮箱 API 的密钥，你自己随便生成一串即可，例如：

```text
CHANGE_ME_RANDOM_TOKEN
```

生产环境建议用更长的随机字符串。

### 4.5 开启 Email Routing

1. 回到 Cloudflare 域名页面。
2. 打开 `Email` / `Email Routing`。
3. 按提示启用 Email Routing。
4. Cloudflare 会要求添加 MX/TXT 记录，按页面提示添加或让 Cloudflare 自动添加。
5. 进入 Routing rules。
6. 找到 Catch-all 地址。
7. Catch-all 选择 `Send to Worker`。
8. Worker 选择刚才创建的 `mail-worker`。
9. 保存。

### 4.6 测试 Worker

把下面命令里的地址和密钥替换成你自己的：

```bash
curl -H "Authorization: Bearer CHANGE_ME_RANDOM_TOKEN" \
  https://mail-worker.your-name.workers.dev/api/health
```

成功应该返回：

```json
{"ok":true}
```

再测试创建邮箱：

```bash
curl -X POST \
  -H "Authorization: Bearer CHANGE_ME_RANDOM_TOKEN" \
  -H "content-type: application/json" \
  -d '{"name":"test123","domain":"yourdomain.com"}' \
  https://mail-worker.your-name.workers.dev/api/create-address
```

成功应该返回：

```json
{"address":"test123@yourdomain.com"}
```

### 4.7 Worker API 一览

所有请求都需要携带请求头：

```text
Authorization: Bearer MAIL_API_TOKEN
```

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/health` | GET | 健康检查，返回 `{"ok":true}` |
| `/api/create-address` | POST | 创建邮箱，返回 `{"address":"test123@yourdomain.com"}` |
| `/api/mails?address=&limit=&offset=` | GET | 按地址查询邮件 |

查询邮件的完整示例：

```bash
curl -H "Authorization: Bearer CHANGE_ME_RANDOM_TOKEN" \
  "https://mail-worker.your-name.workers.dev/api/mails?address=test123@yourdomain.com&limit=10&offset=0"
```

无邮件时返回：

```json
{"results":[]}
```

### 4.8 项目配置与 Worker 配置对应关系

| 项目配置字段 | 对应 Worker 配置 | 说明 |
| --- | --- | --- |
| `mailBaseUrl` | Worker 部署地址 | 例如 `https://mail-worker.your-name.workers.dev` |
| `mailAdminToken` | 环境变量 `MAIL_API_TOKEN` | 必须完全一致 |
| `mailDomain` / `mailDomains` | 环境变量 `MAIL_DOMAIN` | 例如 `yourdomain.com` |

### 4.9 排查与语法检查

Worker 是 ESM 模块，需要按 `.mjs` 后缀检查语法：

```bash
cp cloudflare-email-worker.js /tmp/cloudflare-email-worker.mjs
node --check /tmp/cloudflare-email-worker.mjs
```

更多排查（unauthorized、收不到验证码）见 [10. 常见问题](#10-常见问题)。

## 5. 配置本项目

Linux 默认读取：

```text
config.server.json
```

macOS 默认读取：

```text
config.local.json
```

也可以强制指定配置文件：

```bash
CONFIG_FILE=./config.server.json node index.js 1
```

### 5.1 Linux 推荐配置

打开 `config.server.json`，替换成下面结构：

```json
{
  "heroSmsApiKey": "YOUR_HEROSMS_API_KEY",
  "heroSmsService": "dr",
  "heroSmsCountry": 46,
  "phoneCountryCode": "SE",
  "heroSmsPromptCountrySelection": false,
  "mailProvider": "cloudflare-worker",
  "mailBaseUrl": "https://mail-worker.your-name.workers.dev",
  "mailAdminToken": "YOUR_MAIL_API_TOKEN",
  "mailDomain": "yourdomain.com",
  "mailDomains": [
    "yourdomain.com"
  ],
  "proxyHost": "127.0.0.1",
  "proxyPort": 7897,
  "proxyUsername": "",
  "proxyPassword": "",
  "tokenOutputDirs": [
    "tokens"
  ],
  "useChrome": true,
  "chromePath": "/usr/bin/google-chrome-stable",
  "browserUserDataDir": "browser-profile",
  "browserIncognito": false,
  "browserClearChatGptSession": true
}
```

必须替换的字段：

- `heroSmsApiKey`：HeroSMS API Key。
- `mailBaseUrl`：Cloudflare Worker 地址。
- `mailAdminToken`：Worker 的 `MAIL_API_TOKEN`。
- `mailDomain` / `mailDomains`：你的域名。
- `proxyHost` / `proxyPort`：你的代理地址；没有代理就填空和 `0`。
- `chromePath`：你的 Chrome 路径。

如果没有代理，改成：

```json
{
  "proxyHost": "",
  "proxyPort": 0,
  "proxyUsername": "",
  "proxyPassword": ""
}
```

### 5.2 为什么要配置 browserUserDataDir

`browserUserDataDir` 用来保存 Chrome profile，降低每次都卡 Cloudflare 的概率。

`browserClearChatGptSession: true` 会在每次新注册前清理 ChatGPT 登录态，避免打开页面时还是上一个账号。

这两个建议同时保留：

```json
{
  "browserUserDataDir": "browser-profile",
  "browserIncognito": false,
  "browserClearChatGptSession": true
}
```

## 6. 跑完整流程

在项目目录执行：

```bash
node index.js 1
```

含义：注册 1 个账号，并生成 1 个 token。

批量注册 5 个：

```bash
node index.js 5
```

运行时会看到这些阶段：

1. Phase 1：用 HeroSMS 手机号注册 ChatGPT。
2. Phase 1.5：首次进入 ChatGPT，补 about-you。
3. Phase 2：进入 Codex OAuth，给手机号账号绑定临时邮箱。
4. Phase 3：用邮箱登录 Codex OAuth，换取 token。

成功后会看到类似：

```text
[OAuth] Token 成功保存至: tokens/codex-xxx@yourdomain.com-free.json
[完成] Token 数量 (1) 已达目标 (1)。
```

## 7. 查看产物

账号数据：

```text
accounts.json
```

邮箱绑定记录：

```text
username.json
```

最终 token：

```text
tokens/codex-邮箱-free.json
```

失败记录：

```text
shibai.json
```

运行日志：

```text
logs/
```

## 8. 只补某个阶段

### 8.1 只继续绑定邮箱

如果手机号注册成功，但邮箱绑定失败：

```bash
node index.js --phase2
```

脚本会从 `accounts.json` 里列出账号，让你选择一个继续绑定邮箱。
该模式会停在 Phase 2 邮箱绑定完成的位置，不会继续换 token。

### 8.2 新注册并停在 Phase 2 收尾

如果你要调试“注册完成 + 邮箱绑定完成”之后的逻辑，可以执行：

```bash
node index.js 1 --stop-after-phase2
```

这个命令会：

1. 注册一个手机号账号。
2. 完成首次进入 ChatGPT 的 about-you。
3. 绑定 Cloudflare 临时邮箱。
4. 保存 `accounts.json` 和 `username.json`。
5. 停止在 Phase 2 收尾，不执行 Phase 3。

### 8.3 只补 token

如果邮箱绑定已经成功，但最后没有拿到 token：

```bash
node index.js --phase3
```

脚本会从 `username.json` 里列出邮箱记录，让你选择一个重新走邮箱 OAuth 换 token。

### 8.4 批量补 token

```bash
node index.js --phase8
```

脚本会按 `username.json` 里的记录批量换 token，失败写入 `shibai.json`。

## 9. 导入 sub2api

sub2api 支持 OpenAI Codex session 导入。

单个 token：

1. 打开 sub2api 后台。
2. 进入 Accounts。
3. 新建账号。
4. 平台选择 OpenAI。
5. 类型选择 OAuth / ChatGPT OAuth。
6. 选择 Codex Session Import。
7. 打开本项目生成的 token 文件：

```text
tokens/codex-邮箱-free.json
```

8. 把整个 JSON 内容粘贴进去。
9. 保存。

批量导入：

把多个 token 文件整理成 JSONL，每行一个完整 JSON：

```jsonl
{"access_token":"...","refresh_token":"...","id_token":"...","email":"a@yourdomain.com","type":"codex"}
{"access_token":"...","refresh_token":"...","id_token":"...","email":"b@yourdomain.com","type":"codex"}
```

然后整段粘贴到 sub2api 的 Codex Session Import 文本框。

注意：

- 不要把这个 token 填到 API Key 输入框。
- 必须走 OpenAI OAuth / Codex Session Import。
- `refresh_token` 很重要，缺少它后续无法自动刷新。

## 10. 常见问题

### 10.1 打开就是上一个账号登录态

确认配置里有：

```json
{
  "browserClearChatGptSession": true
}
```

如果还是不行，关闭所有 Chrome 自动化窗口后删除：

```text
browser-profile/
```

再重新运行。

### 10.2 Cloudflare Worker 返回 unauthorized

检查项目配置里的：

```json
{
  "mailAdminToken": "YOUR_MAIL_API_TOKEN"
}
```

必须和 Worker 环境变量 `MAIL_API_TOKEN` 完全一致。

### 10.3 收不到邮箱验证码

按顺序检查：

1. Cloudflare Email Routing 是否启用。
2. MX 记录是否正确。
3. Catch-all 是否设置为 Send to Worker。
4. Worker 是否绑定 D1，绑定名必须是 `DB`。
5. Worker 变量 `MAIL_DOMAIN` 是否是你的域名。
6. `/api/health` 是否返回 `{"ok":true}`。

### 10.4 号码国家不对

如果要固定瑞典：

```json
{
  "heroSmsCountry": 46,
  "phoneCountryCode": "SE",
  "heroSmsPromptCountrySelection": false
}
```

如果想启动时手动选择便宜国家：

```json
{
  "heroSmsPromptCountrySelection": true
}
```

### 10.5 token 保存失败，没有权限

检查 `tokenOutputDirs`。

推荐只保存在项目目录：

```json
{
  "tokenOutputDirs": [
    "tokens"
  ]
}
```

不要填当前用户没有权限写入的目录，例如其他用户的 home 目录。

### 10.6 npm test 失败

当前项目没有测试脚本，`npm test` 本来就是：

```text
Error: no test specified
```

检查语法可以用：

```bash
node --check index.js
node --check src/browserService.js
node --check src/config.js
node --check src/mailProvider.js
node --check src/smsProvider.js
```

Cloudflare Worker 是 ESM，需要按 `.mjs` 检查：

```bash
cp cloudflare-email-worker.js /tmp/cloudflare-email-worker.mjs
node --check /tmp/cloudflare-email-worker.mjs
```

## 11. 不要随便提交或外发的文件

这些文件包含账号、token、日志或浏览器登录态：

```text
accounts.json
username.json
shibai.json
tokens/
logs/
browser-profile/
node_modules/
```

对外发包建议使用 Git 提交包，不要直接压缩整个工作目录。
